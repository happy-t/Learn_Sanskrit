<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.languagelearndemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/new_logo_lauch"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/new_logo_lauch_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LanguageLearnDemo">
        <activity android:name=".PhrasesAct"/>
        <activity android:name=".FamilyName_activity" />
        <activity android:name=".ColorsActivity" />
        <activity android:name=".Numbers" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>